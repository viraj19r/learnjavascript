const name = "hai;4.985;4948";
const transformedArr = name.split(";"); // split(a separator by which we want to separate our string)
console.log(transformedArr);

const arr = ["country", "India"];
const transformedStr = arr.join("-");
console.log(transformedStr);